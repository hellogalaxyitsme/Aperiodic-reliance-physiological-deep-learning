# Paper Files

This folder contains the current LaTeX manuscript and bibliography.

```text
main.tex
references.bib
sn-jnl.cls
sn-nature.bst
Images/
```

`Images/` is duplicated here so that `main.tex` can be compiled from this
folder without editing figure paths.

